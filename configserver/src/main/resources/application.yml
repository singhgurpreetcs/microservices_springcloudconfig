spring:
  application:
    name: "configserver"
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: "file:///D://2025//COURSES//Tests//config"
        #search-locations: "classpath:/config"

server:
  port: 8071


